<?php

$str="This is a String and String is sequence of characters";



// ***************************STRING METHODS****************************

// strlen()
// str_replace()
// strpos()
// substr()
// trim()
// strtolower() / strtoupper()
// explode()
// implode()
// htmlspecialchars()
// md5() / sha1() / hash()
// json_encode($array)  / json_decode($jsonStr)


echo $_COOKIE['favColor'];








// strlen($str)
// Returns the length of a string.

// str_replace("old","new",$str)
// Replaces all occurrences of a search string with a replacement string.

// strpos($str,"word")
// Finds the position of the first occurrence of a substring in a string.

// substr($str,5,8)
// Returns a portion of a string starting from a specified position.

// trim($str)
// Removes whitespace (or other specified characters) from the beginning and end of a string.

// strtolower($str) / strtoupper($str)
// Converts all characters in a string to lowercase or uppercase, respectively.

// explode("-",$str)
// Splits a string into an array based on a delimiter.

// implode("-",$str)
// Joins array elements into a string with a specified delimiter.

// htmlspecialchars($str)
// Converts special characters to HTML entities to prevent XSS attacks.

// md5($str) / sha1($str) / hash('sha256',$str)
// Computes the hash of a string using different algorithms (MD5, SHA1, etc.).

// json_encode($array)  / json_decode($jsonStr)
// ncodes a value to JSON format or decodes a JSON string to a PHP variable.


?>